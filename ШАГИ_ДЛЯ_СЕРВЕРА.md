# 📋 Шаги для выполнения на сервере PS.kz

## Подключение к серверу

Открой **новый терминал** на Mac и выполни:

```bash
ssh root@195.49.215.165
# или
ssh ubuntu@195.49.215.165
```

(Используй того пользователя, под которым работал раньше - судя по логам, это `root`)

---

## Шаг 1: Обнови проект на сервере

```bash
cd ~/resonance-assistant
git pull
```

Если проект был склонирован из другого репозитория, обнови URL:
```bash
cd ~/resonance-assistant
git remote set-url origin https://github.com/artistakss/new-resonance-assistant-bot.git
git pull
```

---

## Шаг 2: Запусти диагностику

```bash
cd ~/resonance-assistant
source .venv/bin/activate
python diagnose.py
```

**Скопируй весь вывод** и отправь мне!

---

## Шаг 3: Посмотри логи сервиса

```bash
sudo journalctl -u resonance-bot.service -n 50 --no-pager
```

**Скопируй весь вывод** и отправь мне!

---

## Шаг 4: Проверь статус сервиса

```bash
sudo systemctl status resonance-bot.service
```

---

## Если нужно пересоздать виртуальное окружение

```bash
cd ~/resonance-assistant
rm -rf .venv
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

---

## Если нужно перезапустить сервис

```bash
sudo systemctl restart resonance-bot.service
sudo journalctl -u resonance-bot.service -f
```

