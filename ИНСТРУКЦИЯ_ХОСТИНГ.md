# 🚀 Инструкция по развертыванию бота на PS Clouds

## Подготовка

### Что нужно иметь:
- ✅ Доступ к серверу PS Clouds (IP: `195.49.215.165`)
- ✅ Логин и пароль для SSH (`ubuntu`)
- ✅ Проект уже готов на твоём Mac

---

## Шаг 1: Подключение к серверу

1. Открой **Терминал** на Mac
2. Подключись к серверу:
   ```bash
   ssh ubuntu@195.49.215.165
   ```
3. Введи пароль (который ты использовал раньше)

---

## Шаг 2: Подготовка сервера

### 2.1. Обнови систему и установи Python 3.11
```bash
sudo apt update
sudo apt install -y python3.11 python3.11-venv python3.11-dev git
```

### 2.2. Перейди в домашнюю директорию
```bash
cd ~
```

---

## Шаг 3: Загрузка проекта на сервер

### Вариант A: Через Git (если проект на GitHub)
```bash
git clone https://github.com/artistakss/resonance-assistant.git
cd resonance-assistant
```

### Вариант B: Через SCP (копирование с Mac)
**На своём Mac** (в новом окне Терминала):
```bash
cd /Users/mac/resonance-assistant-main
scp -r . ubuntu@195.49.215.165:~/resonance-assistant
```

**На сервере** (вернись в SSH):
```bash
cd ~/resonance-assistant
```

---

## Шаг 4: Настройка окружения

### 4.1. Создай виртуальное окружение
```bash
python3.11 -m venv .venv
source .venv/bin/activate
```

### 4.2. Установи зависимости
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4.3. Создай файл `.env`
```bash
nano .env
```

**Вставь туда содержимое из твоего `.env` файла** (скопируй с Mac):
- `BOT_TOKEN`
- `ADMIN_ID`
- `CHECKER_ID`
- `CHANNEL_ID`
- `CHANNEL_LINK`
- `SUBSCRIPTION_PRICE`
- И все остальные переменные

**Сохрани:** `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

---

## Шаг 5: Создание systemd сервиса (для автозапуска)

### 5.1. Создай файл сервиса
```bash
sudo nano /etc/systemd/system/resonance-bot.service
```

### 5.2. Вставь следующее содержимое:
```ini
[Unit]
Description=Resonance Assistant Telegram Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/resonance-assistant
Environment="PATH=/home/ubuntu/resonance-assistant/.venv/bin"
ExecStart=/home/ubuntu/resonance-assistant/.venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Сохрани:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 5.3. Активируй и запусти сервис
```bash
sudo systemctl daemon-reload
sudo systemctl enable resonance-bot.service
sudo systemctl start resonance-bot.service
```

### 5.4. Проверь статус
```bash
sudo systemctl status resonance-bot.service
```

Должно быть: **"active (running)"** ✅

---

## Шаг 6: Просмотр логов

### Смотреть логи в реальном времени:
```bash
sudo journalctl -u resonance-bot.service -f
```

### Смотреть последние 100 строк:
```bash
sudo journalctl -u resonance-bot.service -n 100
```

---

## Шаг 7: Управление ботом

### Остановить бота:
```bash
sudo systemctl stop resonance-bot.service
```

### Запустить бота:
```bash
sudo systemctl start resonance-bot.service
```

### Перезапустить бота:
```bash
sudo systemctl restart resonance-bot.service
```

### Перезагрузить после изменения кода:
```bash
sudo systemctl restart resonance-bot.service
sudo journalctl -u resonance-bot.service -f
```

---

## ⚠️ Важные моменты

1. **Бот должен быть админом канала** с правами:
   - Блокировать участников
   - Разблокировать участников

2. **Проверь права на папку:**
   ```bash
   chmod +x /home/ubuntu/resonance-assistant
   ```

3. **Если бот не запускается:**
   - Проверь логи: `sudo journalctl -u resonance-bot.service -n 50`
   - Убедись, что `.env` заполнен правильно
   - Проверь, что Python 3.11 установлен: `python3.11 --version`

4. **Обновление проекта:**
   ```bash
   cd ~/resonance-assistant
   git pull  # если используешь Git
   # или загрузи файлы заново через SCP
   sudo systemctl restart resonance-bot.service
   ```

---

## 🔧 Решение проблем

### Бот не отвечает:
1. Проверь логи: `sudo journalctl -u resonance-bot.service -f`
2. Проверь, что бот запущен: `sudo systemctl status resonance-bot.service`
3. Проверь токен в `.env`

### Ошибка "ModuleNotFoundError":
```bash
cd ~/resonance-assistant
source .venv/bin/activate
pip install -r requirements.txt
```

### Бот падает при запуске:
- Проверь логи на ошибки
- Убедись, что все переменные в `.env` заполнены
- Проверь права доступа к файлам

---

## ✅ Готово!

После выполнения всех шагов бот будет работать 24/7 на сервере. Даже если ты закроешь SSH-сессию, бот продолжит работать.

**Проверь:** напиши боту `/start` в Telegram — он должен ответить!


