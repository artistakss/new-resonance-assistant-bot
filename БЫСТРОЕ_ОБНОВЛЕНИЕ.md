# 🚀 Быстрое обновление файлов на сервере

## Вариант 1: Скопировать файлы через SCP (рекомендуется)

**На Mac** (в новом окне терминала):

```bash
cd /Users/mac/resonance-assistant-main

# Скопируй только обновленные файлы
scp app/config.py ubuntu@195.49.215.165:~/resonance-assistant/app/config.py
scp diagnose.py ubuntu@195.49.215.165:~/resonance-assistant/diagnose.py
```

---

## Вариант 2: Инициализировать git на сервере

**На сервере:**

```bash
cd ~/resonance-assistant
git init
git remote add origin https://github.com/artistakss/new-resonance-assistant-bot.git
git fetch
git checkout -b main origin/main
# или если есть локальные изменения, которые нужно сохранить:
git pull origin main --allow-unrelated-histories
```

---

## После копирования файлов - запусти диагностику

**На сервере:**

```bash
cd ~/resonance-assistant
source .venv/bin/activate
python3 diagnose.py  # используй python3, а не python
```

