# 🔧 Исправление проблем на сервере

## Проблема 1: Виртуальное окружение сломано

**Решение:** Пересоздать виртуальное окружение

```bash
cd ~/resonance-assistant
rm -rf .venv
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

## Проблема 2: Сервис использует другой путь

Сервис запущен из `/root/resonance-assistant`, а ты работаешь в `/home/ubuntu/resonance-assistant`.

**Вариант A:** Исправить сервис, чтобы использовал путь ubuntu

```bash
sudo nano /etc/systemd/system/resonance-bot.service
```

Измени на:
```ini
[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/resonance-assistant
Environment="PATH=/home/ubuntu/resonance-assistant/.venv/bin"
ExecStart=/home/ubuntu/resonance-assistant/.venv/bin/python3 bot.py
Restart=always
RestartSec=10
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart resonance-bot.service
```

**Вариант B:** Работать в `/root/resonance-assistant` (если там уже всё настроено)

```bash
cd /root/resonance-assistant
# Пересоздать .venv там
rm -rf .venv
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

## После исправления - проверка

```bash
# Проверь, что зависимости установлены
source .venv/bin/activate
python3 -c "import aiogram; print('OK')"

# Проверь статус сервиса
sudo systemctl status resonance-bot.service

# Посмотри логи
sudo journalctl -u resonance-bot.service -f
```

