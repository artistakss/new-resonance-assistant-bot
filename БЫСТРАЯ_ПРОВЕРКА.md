# 🔍 Быстрая проверка доступа к админ-панели

## Проблема: "❌ У вас нет доступа"

### Шаг 1: Проверь свой Telegram ID

1. Напиши боту [@userinfobot](https://t.me/userinfobot) команду `/start`
2. Он покажет твой **ID** (цифры)
3. Убедись, что этот ID совпадает с `ADMIN_ID` в файле `.env`

### Шаг 2: Перезапусти бота полностью

1. В терминале, где запущен бот, нажми `Ctrl+C` (остановка)
2. Подожди 2-3 секунды
3. Запусти снова:
   ```bash
   cd /Users/mac/resonance-assistant-main
   source .venv/bin/activate
   python bot.py
   ```

### Шаг 3: Проверь команду `/admin`

1. Напиши боту `/admin`
2. Если всё ещё "нет доступа" — проверь логи в терминале

### Шаг 4: Если не помогло

**Временное решение:** Я убрал фильтр админов, так что команда `/admin` должна работать для всех. Но лучше исправить доступ.

**Проверь в `.env`:**
- `ADMIN_ID="177748751"` — это твой ID?
- `ALLOWED_ADMINS="177748751,5741639709"` — твой ID должен быть здесь

---

## ✅ После исправления

Когда доступ заработает, можешь:
- Обновлять реквизиты через `/admin`
- Смотреть активных подписчиков
- Просматривать записи на консультации



